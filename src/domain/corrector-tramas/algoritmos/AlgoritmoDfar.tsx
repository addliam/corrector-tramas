import { Corrector } from "./interface/Corrector";
class AlgoritmoDfar implements Corrector {
  constructor() {}
  public corregir(contenido: string[][]): string[][] {
    return [["a", "b"]];
  }
}
export default AlgoritmoDfar;
